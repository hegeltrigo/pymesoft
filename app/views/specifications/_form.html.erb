   

<div class="container">
  <div class="row">
    <div class="col-12">
    <div class="row">
      <div class="col-md-6 m-b-20">

          <%= form_with(model: specification, id: 'specification', local: true) do |form| %>
            <% if specification.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(specification.errors.count, "error") %> prohibited this specification from being saved:</h2>

                <ul>
                <% specification.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            <% end %>

            <div class="form-group">
              <%= form.label 'Nombre' %>
              <%= form.text_field :name, placeholder: "Ej: Talla",  class: 'form-control' %>
            </div>
            
            <button type="submit" class="btn btn-primary">Guardar</button>
            <%= link_to 'Atrás', categories_path, class: 'btn btn-default' %>

        <% end %>
      </div>
    </div>  

    </div>          
  </div>
  
</div>


